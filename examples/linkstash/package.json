{
  "name": "linkstash",
  "version": "0.1.0",
  "type": "module",
  "description": "A tiny link-saver — the workshop starter project.",
  "scripts": {
    "setup": "npm install && node scripts/seed.js",
    "dev": "node src/server.js",
    "start": "node src/server.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "axios": "0.21.1",
    "better-sqlite3": "^11.8.1",
    "express": "^4.21.2",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "prettier": "^3.4.2",
    "vitest": "^2.1.8"
  },
  "engines": {
    "node": ">=20"
  }
}
